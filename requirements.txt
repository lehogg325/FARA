# Serverless function dependencies (Vercel Python runtime).
# The ingest/pipeline packages are NOT deployed — they run in GitHub Actions
# against the raw archive, never on Vercel (docs/deploy.md).
fastapi>=0.115
psycopg[binary]>=3.1
psycopg-pool>=3.2
pydantic>=2
