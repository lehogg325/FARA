[project]
name = "fara-workspace"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["ingest", "pipeline/normalize", "pipeline/extract", "backend"]
